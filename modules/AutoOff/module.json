{
    "autoload": false,
    "userView": true,
    "singleton": false,
    "defaults": {
        "title": "Auto off devices",
        "description": "Turns off automatically devices after a defined time",
        "timeout": 5,
        "device": ""
    },
    "schema": {
        "description": "Automatically turn of devices after N seconds",
        "type": "object",
        "properties": {
            "device": {
                "field": "enum",
                "datasource": "namespaces",
                "enum": "namespaces:devices:deviceId",
                "required": true
            },
            "timeout": {
                "type": "number",
                "required": true
            }
        },
        "required": false
    },
    "options": {
        "fields": {
            "device": {
                "label": "The device to be turned off afte the timeout",
                "datasource": "namespaces",
                "field": "optionLabels",
                "optionLabels": "namespaces:devices:deviceName"
            },
            "timeout": {
                "label": "Timout in seconds"
            }
        }
    }
}
