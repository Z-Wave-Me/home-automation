{
    "dependencies": ["ZWave"],
    "singleton": true,
    "defaults": {
        "title": "Z-Wave device dead detection",
	"description": "Detects Z-Wave dead devices and sends notifications"
    },
    "schema": {
    },
    "options": {
    }
}
